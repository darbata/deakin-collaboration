import PageHeader from "@/components/PageHeader.tsx";

export default function EventsPage() {
    return (
        <PageHeader
            header="Events"
            description="See what others are up to. Found or planning an event? Post it here, to get others excited too"
        />
    )
}